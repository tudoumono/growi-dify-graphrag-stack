# GROWI 環境変数テンプレート
# このファイルをコピーして .env を作成し、各値を設定してください:
#   cp .env.example .env
#
# シークレット生成コマンド:
#   PASSWORD_SEED : openssl rand -base64 24 | tr -d '/'
#   SECRET_TOKEN  : openssl rand -hex 32

APP_SITE_URL=http://localhost:3300

PASSWORD_SEED=changeme
SECRET_TOKEN=changeme
